<style>
  .search-btn{
    background-color: #fe424d !important;
    color: #ffffff;
    border-radius: 25px;
    padding: 0rem .7rem 0rem .7rem;
  }
  .search-btn:hover{
    background-color: #fe424d !important;
    color: #ffffff;
  }
  .fa-magnifying-glass{
   color: white ;
   width: 1vh;

  }
  .search-btn i{
    display: inline;
    margin-right: 0.5rem;
  }
  .search-input{
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  }
</style>

<div class="container-fluid sticky-top">
    <nav class="navbar navbar-expand-md navbar-light bg-light border-bottom ">
        <div class="container-fluid">
          <a class="navbar-brand" href="/listings"><i class="fa-solid fa-not-equal"></i></i></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <a class="nav-link" href="/listings">Explore</a>
            </div>
            
            <div class="navbar-nav ms-auto">
              <form class="d-flex" role="search">
                <input 
                  class="form-control me-2 search-input" 
                  type="search" 
                  placeholder="Search destination" 
                  
                />
                <button class="btn search-btn" type="submit">
                  <i class="fa-solid fa-magnifying-glass"></i><b>Search</b>
                </button>
              </form>
            </div>

            <div class="navbar-nav ms-auto"> <!--ms-auto is to shift this three option to right side-->
              <a class="nav-link" href="/listings/new">Hostel Your Home</a>
             <% if(!currUser){ %>
              <a href="/signup" class="nav-link"><b>Signup</b></a>
              <a href="/login" class="nav-link"><b>Login</b></a>
              <% }%>
              <% if(currUser){%>
              <a href="/logout" class="nav-link">Logout</a>
              <%}%>
            </div>
          </div>
        </div>
      </nav>
</div>